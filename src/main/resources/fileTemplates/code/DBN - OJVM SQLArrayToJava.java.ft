    private static ${JAVA_COMPLEX_TYPE}[]${SQUARE_BRACKETS} ${CONVERTER_METHOD_NAME}(java.sql.Array obj)  throws java.sql.SQLException{
        if (obj == null) return null;
        $BR
        Object[] objArray = (Object[]) obj.getArray();
        if (objArray == null) return null;
        $BR
        ${JAVA_COMPLEX_TYPE}[]${SQUARE_BRACKETS} javaObj = new ${JAVA_COMPLEX_TYPE}[objArray.length]${SQUARE_BRACKETS};
        for (int i = 0; i < objArray.length; i++) {
            ${ITERATION_CODE}
        }
        $BR
        return javaObj;
    }
    $BR
