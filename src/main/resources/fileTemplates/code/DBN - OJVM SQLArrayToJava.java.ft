public static ${JAVA_COMPLEX_TYPE}[]${SQUARE_BRACKETS} ${CONVERTER_METHOD_NAME}(java.sql.Array obj) {
    if (obj == null) return null;

    Object[] ${OBJ_ARRAY};
    try {
        ${OBJ_ARRAY} = (Object[]) obj.getArray();
    } catch (java.sql.SQLException e) {
        return null;
    }
    if (objArray == null) return null;

    ${JAVA_COMPLEX_TYPE}[]${SQUARE_BRACKETS} ${JAVA_OBJECT} = new ${JAVA_COMPLEX_TYPE}[${OBJ_ARRAY}.length]${SQUARE_BRACKETS};
    for (int ${ITERATOR} = 0; ${ITERATOR} < ${OBJ_ARRAY}.length; ${ITERATOR}++)
        ${ITERATION_CODE}

    return ${JAVA_OBJECT};
}
