#set($h1 = '#')
#set($h2 = '##')
#set($h3 = '###')
$h1 ${SERVER_NAME}

$h2 1. Overview

#if($IS_CONTAINER)
${SERVER_NAME} is a containerized MCP server generated by Database Navigator and built with Micronaut.
The image contains a GraalVM native executable, so no Java installation is needed at run time.
It runs on a Docker-compatible runtime with the same CPU architecture as the builder.
#else
${SERVER_NAME} is a native executable MCP server generated by Database Navigator,
built with Micronaut and compiled ahead-of-time with GraalVM.
It requires no Java installation to run and is specific to the operating system and CPU architecture
where it was built (rebuild it on another platform before running it there).
#end

It connects to Oracle Database and exposes your SQL queries as MCP tools.

The server communicates over **streamable HTTP**: the MCP endpoint is served on
`http://127.0.0.1:${HTTP_PORT}/mcp`.

---

$h2 2. Running the server

#if($IS_CONTAINER)
This server was built as a **container image**: `${IMAGE_NAME}` (registered with your local
Docker-compatible runtime). The `${MOUNT_DIR}/` folder next to this README holds
`mcp-config.yaml` and the `wallet/` folder — mount it read-only at `/config`. Run this from
inside this directory (the relative path only resolves from here):

```
docker run -d --name ${SERVER_NAME} -p 127.0.0.1:${HTTP_PORT}:${HTTP_PORT} -e MICRONAUT_SERVER_HOST=0.0.0.0 -v ./${MOUNT_DIR}:/config:ro ${IMAGE_NAME} --config=/config/mcp-config.yaml
```

Notes:
- `podman` works identically to `docker`.
- The published port is limited to `127.0.0.1` so the unauthenticated MCP endpoint is not exposed to the network.
- `MICRONAUT_SERVER_HOST=0.0.0.0` is required: the baked-in configuration binds
  `127.0.0.1`, which is unreachable from outside the container.
- Credentials are **mounted at run time, never baked into the image** — the image is
  generic and safe to push to a registry; the mounted directory is the sensitive part.
- **The image targets the builder platform.** It will not run on a different CPU
  architecture — see section 6 for other targets.
#else
Start the executable directly:

```
./${EXECUTABLE_NAME}
```
#end

#if(!$IS_CONTAINER)
Use `--config=` to load a configuration file from another location:

```
./${EXECUTABLE_NAME} --config=/path/to/mcp-config.yaml
```

By default, the native executable loads `mcp-config.yaml` from beside the executable.
When running the generated JAR, it loads the same file from the working directory.
`mcp-config.yml` is also accepted if no `.yaml` file is present.
#end

Restart the server after editing the YAML. The HTTP port is part of `application.yml`, which is baked into the executable;
change the server definition and rebuild to use a different port.

---

$h2 3. Custom Tool Framework

The server loads datasource and tool definitions from `mcp-config.yaml`.
You can edit this file after generation to adjust SQL, descriptions, and parameters — no rebuild needed.
`datasource` and `tools` are top-level sections:

```yaml
datasource:
  wallet-dir: /path/to/wallet
  url: jdbc:oracle:thin:@database_high
  # username: optional_user
  # password: optional_password

tools:
  countries-in-region:
    description: List countries in a region
    statement: |
      select country_id, country_name
      from countries
      where region_id = :region_id
    parameters:
      - name: region_id
        type: integer
        description: Region ID
        required: true
```

A tool entry supports:
- `description`: tool description
- `statement`: SQL statement (read-only query that returns rows)
- `parameters`: optional list of bind parameters

Notes:
- Tool SQL is executed with query semantics (`executeQuery`), so use `SELECT`-style statements that return a result set.
- DML/DDL/anonymous PL/SQL blocks are not supported in this generated server flow.

Parameter fields:
- `name`: bind variable name (without `:`)
- `type`: JSON schema type (`string`, `integer`, `number`, `boolean`)
- `format`: optional metadata for string types (for example `date`)
- `description`: optional parameter description shown to the MCP client
- `required`: whether this parameter is required

$h3 3.1. Generated tools

#foreach($tool in $TOOLS)
- **${tool.name}** — ${tool.description}
#end

---

$h2 4. Credentials and Security

Database credentials are stored in the `wallet/` directory (Oracle auto-login wallet, `cwallet.sso`).
- The builder creates the wallet directory and files with owner-only permissions; keep the directory private and do not commit it.
- To use different credentials without the wallet, uncomment `username`/`password` under `datasource`
  in `mcp-config.yaml`. Values placed there take precedence over the wallet.

---

$h2 5. Rebuilding from source

The `source-project/` directory contains the complete Maven project used to build this server.

#if($IS_CONTAINER)
Rebuilding the **container image** requires Docker or Podman. The generated multi-stage Dockerfile
provides Maven, GraalVM JDK 25, and the native C toolchain inside the builder container.

```
docker build -t ${IMAGE_NAME} source-project/
```
#else
Rebuilding the **native executable** requires:
- **GraalVM JDK 25** configured as the Maven JVM — `native-image` is invoked during the build.
- A platform C toolchain (Xcode command-line tools on macOS, Visual Studio Build Tools on Windows,
  gcc/glibc-devel on Linux).

```
mvn clean package -Dpackaging=native-image
```
#end

To build a regular runnable JAR instead, use JDK 25. GraalVM and a native C toolchain are not required:

```
mvn clean package
java -jar target/${SERVER_NAME}-1.0.jar
```

The GraalVM reachability metadata for the Oracle JDBC driver and wallet support lives in
`src/main/resources/META-INF/native-image/` — keep it in sync with the Oracle driver version
declared in `pom.xml`.

---

$h2 6. Building for other platforms and architectures

Native executables (and the container images holding them) are locked to the OS and CPU
architecture they were built on. GraalVM does not cross-compile. To target a different
architecture (for example an `amd64` server from an Apple Silicon machine), build
`source-project/Dockerfile` **on a machine or CI runner of the target architecture**:

```
docker build -t ${IMAGE_NAME} source-project/
```

The Dockerfile is fully self-contained (multi-stage: Oracle GraalVM builder + slim runtime)
and needs no local GraalVM or Maven — only a container runtime. Native compilation needs
roughly 6-8 GB of memory on the build machine. Run the resulting image with the same
`docker run` command shown in section 2, mounting a directory that contains
`mcp-config.yaml` (with `wallet-dir: /config/wallet`) and the `wallet/` folder.

Do not rely on emulated cross-architecture builds (`--platform`) — native-image under
emulation is extremely slow and memory-hungry; build natively on the target architecture
instead.

Note: do not run `mvn -Dpackaging=docker-native` inside `source-project/` while the
`Dockerfile` is present — the Micronaut Maven plugin would treat it as an override of its
own packaging and the build fails. Use `docker build` (as above), or remove/rename the
`Dockerfile` first.
