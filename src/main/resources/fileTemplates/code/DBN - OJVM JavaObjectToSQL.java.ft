    private static java.sql.Struct ${CONVERTER_METHOD_NAME}(${JAVA_COMPLEX_TYPE} javaObj) throws java.sql.SQLException {
        if (javaObj == null) return null;
        $BR
        java.lang.Object[] objArray = new java.lang.Object[${TOTAL_FIELDS}];
#foreach($FIELD_ASSIGNMENT in $FIELD_ASSIGNMENTS)
        $FIELD_ASSIGNMENT
#end
        $BR
        oracle.jdbc.driver.OracleDriver driver = new oracle.jdbc.driver.OracleDriver();
        java.sql.Connection connection = driver.defaultConnection();
        java.sql.Struct returnObj = connection.createStruct("${SQL_OBJECT_TYPE}", objArray);
        $BR
        return returnObj;
    }
    $BR