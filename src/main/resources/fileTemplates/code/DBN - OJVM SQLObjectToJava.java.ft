    private static ${JAVA_COMPLEX_TYPE} ${CONVERTER_METHOD_NAME}(java.sql.Struct obj) throws java.sql.SQLException {
        if (obj == null) return null;
        Object[] objArray = obj.getAttributes();
        $BR
        ${JAVA_COMPLEX_TYPE} javaObj = new ${JAVA_COMPLEX_TYPE}();
#foreach($FIELD_ASSIGNMENT in $FIELD_ASSIGNMENTS)
        $FIELD_ASSIGNMENT
#end
        $BR
        return javaObj;
    }
    $BR
