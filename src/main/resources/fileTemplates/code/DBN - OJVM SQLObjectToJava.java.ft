public static ${JAVA_COMPLEX_TYPE} ${CONVERTER_METHOD_NAME}(java.sql.Struct obj) {
        if (obj == null) return null;

        Object[] ${OBJ_ARRAY};
        try {
                ${OBJ_ARRAY} = obj.getAttributes();
        } catch (java.sql.SQLException e) {
                return null;
        }

        ${JAVA_COMPLEX_TYPE} ${JAVA_OBJECT} = new ${JAVA_COMPLEX_TYPE}();
    #if($FIELD_ASSIGNMENTS)
        #foreach($assignment in $FIELD_ASSIGNMENTS)
            $assignment
        #end
    #end

        return javaObj;
}
