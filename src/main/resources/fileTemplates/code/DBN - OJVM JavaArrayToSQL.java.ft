    private static java.sql.Array ${CONVERTER_METHOD_NAME}(${JAVA_COMPLEX_TYPE}[]${SQUARE_BRACKETS} javaObj) throws java.sql.SQLException {
        if (javaObj == null) return null;
        $BR
        java.lang.Object[] objArray = new java.lang.Object[javaObj.length];
        for (int i = 0; i < javaObj.length; i++) {
            ${ITERATION_CODE}
        }
        $BR
        oracle.jdbc.driver.OracleDriver driver = new oracle.jdbc.driver.OracleDriver();
        oracle.jdbc.OracleConnection connection = (oracle.jdbc.OracleConnection) driver.defaultConnection();
        java.sql.Array returnArray = connection.createARRAY("${SQL_OBJECT_TYPE}", objArray);
        $BR
        return returnArray;
    }
    $BR